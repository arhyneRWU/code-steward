__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
dist/
build/
*.egg-info/
.code-steward/
.code-review-graph/
.DS_Store
# Repo-local Claude assets are tracked; only agent worktrees and the
# machine-local plugin toggle are ignored.
.claude/worktrees/
.claude/settings.local.json

# Coverage output, written by `coverage run`.
.coverage
.coverage.*
htmlcov/
coverage.json
